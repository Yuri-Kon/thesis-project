# 术语统一对照（2026-03-16）

本次对 Week11/Week12 相关中文文档执行统一，后续建议沿用以下术语。

| 概念 | 统一写法 | 说明 |
|---|---|---|
| fallback | 回退 | 仅在代码字段/文件名中保留 `fallback` |
| fallback safety | 回退保障 | 避免与“兜底”混用 |
| gate | 门禁 | 用于通过/阻断判定 |
| threshold | 阈值 | 数值条件统一称“阈值” |
| gate threshold | 门禁阈值 | 避免“门槛”与“阈值”混用 |
| replay | 回放 | 避免“重放”与“回放”混用 |
| runbook | 操作手册 | 标题统一中文，不再使用 Runbook |
| governance review | 治理复核 | “复核”保持统一 |
| `suffix_replan` metric | `suffix_replan` 前缀保持率 | 统一使用下划线形式的代码标识 |
| Project status `In Progress` | `In Progress`（进行中） | 状态值保留原字段，中文在括注说明 |
| Project status `Done` | `Done`（已完成） | 状态值保留原字段，中文在括注说明 |

## 扫描结论

已对本次改写范围内的 Markdown 扫描 `门槛/兜底/Runbook/重放/Suffix replan/suffix-replan` 等混用词，未发现残留。
