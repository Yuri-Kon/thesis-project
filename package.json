{
  "name": "thesis-project.dev",
  "private": true,
  "scripts": {
    "build:ui": "tsc -p tsconfig.json",
    "check:ui": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
